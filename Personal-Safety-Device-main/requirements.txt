# Core
SQLAlchemy>=1.4
requests>=2.25
pyserial>=3.5
cryptography>=41.0

# Audio (YAMNet AI model)
numpy>=1.21
sounddevice>=0.4
# NOTE: YAMNet audio detection requires ONE of these:
#   Python 3.9-3.12 (RECOMMENDED on Pi):  pip install tflite-runtime
#   Python 3.13+:  pip install tensorflow
# WARNING: full tensorflow on ARM/Pi causes segfaults during ChaCha20
# encryption. Use tflite-runtime on Pi for stable SOS uploads.
# If neither is installed, YAMNet falls back to keyboard-trigger mode.

# Raspberry Pi hardware
RPi.GPIO
smbus2
# NOTE: spidev is only needed if SX1278 LoRa hardware is connected.
# If not installed, LoRa silently disables (no crash, no simulation).
spidev

# Sensors
adafruit-circuitpython-bno055
adafruit-circuitpython-busdevice
# NOTE: max30102 has no PyPI package for Python 3.13+
# Install manually if available:  pip install max30102
# If not installed, the code auto-falls back to FakeHeartRate (won't crash)

# Camera (Pi Camera Module via CSI)
# NOTE: Uses rpicam-vid CLI (pre-installed on Raspberry Pi OS).
# No Python camera library needed — rpicam-vid is called as a subprocess.
# On non-Pi systems, the code auto-falls back to FakeCameraRecorder.