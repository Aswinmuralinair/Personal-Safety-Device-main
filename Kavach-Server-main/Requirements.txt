# Kavach Server v4.0 — Dependencies
# Install: pip install -r Requirements.txt

# ── Core Flask ──────────────────────────────────────────────────────────────
Flask>=2.3
Flask-SQLAlchemy>=3.0
Flask-Cors>=4.0
Werkzeug>=2.3
itsdangerous>=2.0

# ── Encryption (ChaCha20-Poly1305 for device telemetry) ─────────────────────
cryptography>=41.0

# ── Firebase Cloud Messaging (FCM) — push notifications ─────────────────────
# Optional: only needed if you set FIREBASE_CREDENTIALS env var.
# If not installed, push notifications are silently disabled.
# Uncomment when ready: pip install firebase-admin
# firebase-admin>=6.0
